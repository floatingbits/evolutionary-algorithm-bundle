<form method="post" action="{{ path('evolutionary_algorithm_tournament_type_delete', {'id': tournament_type.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ tournament_type.id) }}">
    <button class="btn btn-danger">Delete</button>
</form>
